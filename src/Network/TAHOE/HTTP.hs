module Network.TAHOE.HTTP where

import Data.Either
import Data.Maybe

import Network.TAHOE.Monad
import Network.TAHOE.Types

import Data.Text (Text)

type URI = Text

{-
createObject uri dat = undefined

createDirectory fs (Just fxs) = undefined

createFile dir dat = undefined

-- | Higher level


upload dir file = undefined

attach dir file = undefined

get obj = undefined

-- | ct is content-type
put obj ct file = undefined

mkdir obj = undefined

del obj = undefined

list dir = undefined
-}
